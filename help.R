# Making connection
# A connection is needed within RStudio and the airlines database for
# the following problems. Here is how we would do it.


# dbConnect(RMySQL::MySQL(),
# dbname = "airlines",
# host = "scidb.smith.edu",
# user = "mth292",
# password = "RememberPi")